<h1><%= @question.title %></h1>
 
<%= link_to "Edit Question", edit_question_path, class: 'btn btn-success' %>
<%= link_to "Delete Question", @question, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this question?' } %>

<div class="row">
  <div class="col-md-8">
    <p class="lead"><%= @question.body %> <%= @question.resolved %></p>
  </div>
</div>