$('.call_voteing').html("<%= escape_javascript(render partial: 'votes/voter', locals: { post: @post }) %>");
$('.vote_total').html("<%= escape_javascript(pluralize(@post.points, 'point')) %>");
$('.vote_up').html("<%= escape_javascript(pluralize(@post.up_votes, 'up vote')) %>");
$('.vote_down').html("<%= escape_javascript(pluralize(@post.down_votes, 'down vote')) %>");