<h1>New Questions</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for Questions:</p>
    <ul>
      <li>Make sure the question is inappropriate.</li>
      <li>Drink cocacola.</li>
      <li>Do onto others before.</li>.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |q| %>
      <div class="form-group">
        <%= q.label :title %>
        <%= q.text_field :title, class: 'form-control', placeholder: "Enter question title" %>
      </div>
      <div class="form-group">
        <%= q.label :body %>
        <%= q.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter body text" %>
      </div>
      <div class="form-group">
        <%= q.label :resolved, class: 'checkbox' do %>
          <%= q.check_box :resolved %> Resolved
        <% end %>
      </div>
      <%= q.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>